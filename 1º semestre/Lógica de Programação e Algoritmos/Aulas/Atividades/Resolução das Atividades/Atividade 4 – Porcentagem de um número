/*
(Atividade 4 – Porcentagem de um número)
Crie um programa capaz de calcular e exibir o percentual correspondente a um valor específico
Contexto: O programa vai pedir para o usuário digitar um determinado valor. Após isso irá pedir que digite a porcentagem. 
O Computador irá calcular a porcentagem do valor e exibir.
Exemplo: valor digitado -> 100
porcentagem digitada -> 30%
resultado -> 30
Observação: não utilizamos o símbolo % na programação. Utilize cálculo matemático.

*/

programa {
  funcao inicio() {
    real numero, porcentagem, resultado

    escreva("Digite um número [de 1 a 10000]: ")
    leia(numero)

    escreva("Digite uma porcentagem [de 1 a 100]: ")
    leia(porcentagem)

    resultado = numero * (porcentagem / 100)

    escreva("O valor do processo é: ", resultado)
  }
}
